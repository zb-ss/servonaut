# Runtime dependencies mirrored from pyproject.toml for the desktop shell.
boto3
tabulate
textual>=8.0.0
cryptography>=42.0
bcrypt>=3.2
pynacl>=1.5
httpx>=0.25.0
httpx-sse>=0.4
keyring>=24

# Supported non-desktop integrations bundled in every desktop shell target.
mcp>=1.0.0,<2
ovh
hcloud>=2.0

# Desktop-exclusive runtime dependencies.
textual-serve==1.1.3
pywebview==6.2.1
aiohttp==3.14.3
aiohttp-jinja2>=1.6

# Build and evidence tooling retained in the isolated build environment.
pyinstaller==6.22.3
pyinstaller-hooks-contrib==2026.7
cyclonedx-bom==7.3.1
