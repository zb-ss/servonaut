# Runtime dependencies mirrored from pyproject.toml for the standalone CLI.
boto3
tabulate
textual>=8.0.0
cryptography>=42.0
bcrypt>=3.2
pynacl>=1.5
httpx>=0.25.0
httpx-sse>=0.4
keyring>=24

# Supported non-desktop integrations bundled in every standalone CLI target.
mcp>=1.0.0,<2
ovh
hcloud>=2.0

# Build and evidence tooling retained in the isolated build environment.
pyinstaller==6.22.3
pyinstaller-hooks-contrib==2026.7
cyclonedx-bom==7.3.1
